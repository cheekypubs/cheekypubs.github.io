---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <!-- Main pages -->
  <url>
    <loc>{{ site.url }}/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>{{ site.url }}/about/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
  </url>
  <url>
    <loc>{{ site.url }}/submit-info/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
  </url>

  <!-- Story pages -->
  {% for story in site.stories %}
  <url>
    <loc>{{ story.url | absolute_url }}</loc>
    <lastmod>{{ story.date | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.9</priority>
  </url>
  {% endfor %}

  <!-- Author pages -->
  {% assign authors = site.stories | map: "author" | uniq | sort %}
  {% for author in authors %}
    {% assign author_slug = author | slugify %}
    <url>
      <loc>{{ site.url }}/pages/authors/{{ author_slug }}/</loc>
      <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
      <changefreq>monthly</changefreq>
      <priority>0.7</priority>
    </url>
  {% endfor %}

  <!-- Tag pages -->
  {% assign tag_slugs = "" | split: "" %}
  {% for story in site.stories %}
    {% for tag in story.tags %}
      {% assign tag_slug = tag | slugify %}
      {% unless tag_slugs contains tag_slug %}
        {% assign tag_slugs = tag_slugs | push: tag_slug %}
        <url>
          <loc>{{ site.url }}/pages/tags/{{ tag_slug }}/</loc>
          <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
          <changefreq>monthly</changefreq>
          <priority>0.7</priority>
        </url>
      {% endunless %}
    {% endfor %}
  {% endfor %}
</urlset>
